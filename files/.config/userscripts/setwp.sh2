#!/bin/bash

if [ -n "$1" ]; then
	if [ ! -f "$1" ] && exit 1
    echo $1 > wppath
    echo Saved Wallpaper "$1"
fi

WP=$(< wppath)

swww img "$WP" --transition-type fade --transition-fps 60 --transition-duration 2
